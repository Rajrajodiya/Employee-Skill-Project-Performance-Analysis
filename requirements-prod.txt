# ═════════════════════════════════════════════════════════════════════════════
#  ESPPA — Production Requirements (Vercel preview-optimized)
#  Vercel uses this file for the deployment build pipeline.
#  For preview, only Django + web deps are included to keep the
#  serverless bundle under the 500 MB limit.
#
#  ML libraries (pandas, numpy, scikit-learn, xgboost, matplotlib)
#  are excluded from preview. The app gracefully degrades when they
#  are missing — ML features show helpful error messages.
#
#  For full local dev / production, use requirements.txt instead.
# ═════════════════════════════════════════════════════════════════════════════

# ── Core Web Framework ───────────────────────────────────────────────────────
Django>=4.2,<5.0
gunicorn>=21.2.0

# ── Django Addons ─────────────────────────────────────────────────────────────
django-crispy-forms>=2.0
crispy-bootstrap5>=1.0
django-widget-tweaks>=1.5.0
django-cotton>=2.7

# ── REST API ──────────────────────────────────────────────────────────────────
djangorestframework>=3.14.0
drf-spectacular>=0.27.0

# ── Environment Management ────────────────────────────────────────────────────
python-decouple>=3.8            # Required by settings/base.py at module level

# ── Production Serving ────────────────────────────────────────────────────────
whitenoise>=6.5.0                # Static file serving in production
psycopg2-binary>=2.9.0           # PostgreSQL adapter (used by prod settings)
