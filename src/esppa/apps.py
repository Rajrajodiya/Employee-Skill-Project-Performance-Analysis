from django.apps import AppConfig


class EsppaConfig(AppConfig):
    """Configuration for the ESPPA application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'esppa'
    verbose_name = 'ESPPA - Employee Performance Analyzer'
